import React from 'react';

const Chat = () => {
    // получаем сообщения, отправителя сравниваем с юзером

    return (
        <div>Chat</div>
    );
};

// скорее всего observable

export default Chat;
